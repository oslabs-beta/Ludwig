"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getAxisData = getAxisData;

var _typeHelpers = require("../../../victory-util/type-helpers");

function getAxisData(data, axis) {
  return data.reduce(function (acc, datum) {
    if ((0, _typeHelpers.isKeyValueObject)(datum)) {
      var value = datum[axis];

      if (value && typeof value !== "string") {
        acc.push(value);
      }
    }

    return acc;
  }, []);
}