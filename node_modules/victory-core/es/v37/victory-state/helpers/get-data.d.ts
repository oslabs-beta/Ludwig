import { DatumValue } from "../../../types/prop-types";
import { VictoryProviderProps } from "../types";
declare type DataProps = Pick<VictoryProviderProps, "data" | "x" | "y" | "sortKey" | "sortOrder" | "samples">;
export interface FormattedDatum {
    x: DatumValue;
    y: DatumValue;
    _x: number | Date;
    _y: number | Date;
    xName?: string;
    yName?: string;
    [key: string]: DatumValue;
}
export declare function getData({ x: xAccessor, y: yAccessor, sortKey, sortOrder, data, }?: DataProps): FormattedDatum[];
export {};
//# sourceMappingURL=get-data.d.ts.map